# Story 5.9: mapping editor UI (persist config)

Status: ready-for-dev

## Story

As an operator,
I want a field-mapping editor UI that persists configuration,
so that 同期ごとに再設定せずに済む。

## Acceptance Criteria

1. マッピング表（CSVの列→ドメインフィールド）を編集・保存・読み込み

## Dev Agent Record

### Context Reference

- docs/stories/5-9-story.context.xml

