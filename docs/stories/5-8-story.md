# Story 5.8: import job history with filters and retry

Status: ready-for-dev

## Story

As an operator,
I want to view import job history with filters and retry failed jobs,
so that 過去の同期状況を確認し失敗時に再実行できる。

## Acceptance Criteria

1. ジョブ一覧（期間/状態/種別でフィルタ）
2. 失敗ジョブに「再実行」ボタン（ダミーでも可）

## Dev Agent Record

### Context Reference

- docs/stories/5-8-story.context.xml

